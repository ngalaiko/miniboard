{
  "name": "Miniboard",
  "version": "1.0.0",
  "license": "MIT",
  "main": "src/index.ts",
  "scripts": {
    "watch": "rm -rf ./dist && mkdir dist && cp -r static dist/ && NODE_ENV=development rollup --config --watch",
    "build": "rm -rf ./dist && mkdir dist && cp -r static dist/ && NODE_ENV=production rollup --config"
  },
  "devDependencies": {
    "@rollup/plugin-commonjs": "^11.0.0",
    "@rollup/plugin-node-resolve": "^9.0.0",
    "@rollup/plugin-replace": "^2.3.1",
    "rollup": "^1.32.0",
    "rollup-plugin-bundle-html": "^0.2.2",
    "rollup-plugin-svelte": "^5.1.1",
    "rollup-plugin-terser": "^5.1.3",
    "rollup-plugin-typescript2": "^0.26.0",
    "svelte": "^3.19.1",
    "svelte-feather-icons": "^3.2.2",
    "svelte-preprocess": "^3.4.0",
    "typescript": "^3.8.2"
  },
  "dependencies": {
    "svelte-infinite-scroll": "^1.0.0",
    "svelte-routing": "^1.4.0"
  }
}
